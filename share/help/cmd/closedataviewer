cdv / closedataviewer         Closes the data viewer window

   Format:
      ;cdv                    Closes the window

   User commands:
      cdv / cov / closeviewer / closedataviewer / closeobjectviewer
