Delregion (string)

   Synopsis:
      Deletes the world model region whose name is 'string'

   Notes:
      Returns 1 if the operation is successful, 0 if not.
      To delete all temporary regions together, see the Deltempregions ()
         function.
