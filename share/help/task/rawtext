The RawText task is a debugging task. It displays all text received from the
   world, including special 'escape sequence' codes that are normally
   invisible to the user, in a separate task window.
